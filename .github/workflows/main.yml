name: Workflow Trigger Test

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test workflow
        run: echo "GitHub Actions is working!"
